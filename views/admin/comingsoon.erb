<div class="white-ground">
	<div id="tag">
		<div class="container">
			<h2 style="font-size: 2em; letter-spacing: -1px;">Dinamec<span style="font-weight: 300; color: #95989c;">Systems</span></h2>
		</div>
		<div class="container master-padding">
			<h1>Customer Section</h1>
			<hr />
			<h5>Hello <% if session[:admin] %>
   			 <%= @user = User.get(session[:admin]).first_name %> <%= @user = User.get(session[:admin]).last_name %><br />
				 <% elsif session[:customer] %>
				 <%= @user = User.get(session[:customer]).first_name %> <%= @user = User.get(session[:customer]).last_name %><br />
				 <% end %>
			</h5>
			<p>The Customer section is in development.</p>
			<p>Today is <%= Time.now.strftime("%A %B %d, %Y") %></p>
			<br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br />
		</div>
	</div>
</div>