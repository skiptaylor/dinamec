<div class="white-ground">
	<div class="container">
		<h2 style="font-size: 2em; letter-spacing: -1px;">Dinamec<span style="font-weight: 300; color: #95989c;">Systems</span></h2>
	</div>
	<div class="container master-padding">
		<p class="pull-right"><a class="btn btn-sm btn-primary" href="<%= request.referrer %>">Profile</a></p>
		<h1 style="margin-bottom: .7em;"><%= @machine = Machine.get(params[:id]).name %> Parts <small></small></h1>
		<div class="row">
			<div class="col-md-12">
				<table class="table table-striped table-condensed table-bordered table-hover">
					<% @machine = Machine.get(params[:id]) %>
					<tr style="font-size: .9em;">
						<th style="text-align: center;">Order</th>
						<th>Part</th>
						<th style="text-align: center;">Qty</th>
						<th>Part Number</th>
						<th>Description</th>
						<th style="text-align: center;"></th>
					</tr>
					<% @machine = Machine.get(params[:id]) %>
					<% @part.each do |part| %>
					<% if part.machine_id == @machine.id %>
					<tr style="font-size: .9em;">
						<td> </td>
						<td><%= part.part %></td>
						<td style="text-align: center;"><%= part.quantity %></td>
						<td><%= part.part_number %></td>
						<td><%= part.description %></td>
						<td><small class="temp"><%= part.machine_id %></small></td>
					</tr>
					<% end %>
					<% end %>
				</table>
			</div>
		</div>
	</div>
</div>
